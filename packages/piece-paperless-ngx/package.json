{
  "name": "@powerhousedao/piece-paperless-ngx",
  "version": "0.1.0",
  "description": "Activepieces piece for paperless-ngx: upload, search, tag and react to documents in a self-hosted archive.",
  "main": "dist/src/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && node scripts/bundle.mjs",
    "publish:bundle": "pnpm run build && npm publish ./dist",
    "bundle": "node scripts/bundle.mjs",
    "test": "pnpm run build && vitest run",
    "test:watch": "vitest",
    "lint": "oxlint --type-aware --type-check",
    "lint:fix": "oxlint --type-aware --type-check --fix",
    "tsc": "tsc"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "activepieces",
    "paperless-ngx",
    "documents"
  ],
  "license": "AGPL-3.0-only",
  "dependencies": {
    "@activepieces/pieces-framework": "0.32.0",
    "@activepieces/shared": "0.95.1"
  },
  "devDependencies": {
    "@powerhousedao/reactor-connectors": "workspace:*",
    "@types/node": "^24.13.3",
    "esbuild": "0.25.12",
    "oxlint": "1.70.0",
    "oxlint-tsgolint": "0.23.0",
    "typescript": "5.9.3",
    "vitest": "4.1.1"
  }
}
